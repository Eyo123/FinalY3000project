<h2 class="page-header"><%= student.name %></h2>
<div class="btn-row">
  <a class="btn btn-primary" href="/students/<%= student.id %>/edit">Edit</a>
  <%- include('../partials/deleteForm',
              { url: `/students/${student.id}` }) %>
</div>
<% if (appointmentsByStudent.length > 0) { %>
  <h2 class="page-header">Appointments By Student</h2>
  <%- include('../partials/appointmentGrid', { appointments: appointmentsByStudent }) %>
<% } %>